const UPSTREAM = "https://football-predict.pages.dev/api/daily-jingcai";
const HEADERS = {"content-type":"application/json; charset=utf-8","cache-control":"public, max-age=120, s-maxage=120","access-control-allow-origin":"*"};
const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:HEADERS});
const n=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
function normalize(a){const s=a.reduce((x,y)=>x+y,0);return s>0?a.map(x=>x/s):null}
function argmax(a){return a.indexOf(Math.max(...a))}
function oldProb(m){return normalize([n(m.homeWinProb??m.spf?.homeWinProb),n(m.drawProb??m.spf?.drawProb),n(m.awayWinProb??m.spf?.awayWinProb)])}
function marketProb(m){const a=[n(m.sp_win),n(m.sp_draw),n(m.sp_lose)];return a.every(x=>x>1)?normalize(a.map(x=>1/x)):null}
function eloProb(m){const eh=n(m.home_elo??m.homeElo??m.elo?.home,NaN),ea=n(m.away_elo??m.awayElo??m.elo?.away,NaN);if(!Number.isFinite(eh)||!Number.isFinite(ea))return null;const raw=1/(1+Math.pow(10,-((eh+65)-ea)/400)),draw=clamp(.30-Math.abs(raw-.5)*.22,.17,.30);return normalize([raw*(1-draw),draw,(1-raw)*(1-draw)])}
function factorial(k){let r=1;for(let i=2;i<=k;i++)r*=i;return r}
function pois(k,l){return Math.exp(-l)*Math.pow(l,k)/factorial(k)}
function matrix(lh,la,dc=false){if(!(lh>0&&la>0))return null;let h=0,d=0,a=0,total=0,scores=[];for(let i=0;i<=8;i++)for(let j=0;j<=8;j++){let p=pois(i,lh)*pois(j,la);if(dc){const rho=-.08;if(i===0&&j===0)p*=1-lh*la*rho;else if(i===0&&j===1)p*=1+lh*rho;else if(i===1&&j===0)p*=1+la*rho;else if(i===1&&j===1)p*=1-rho}p=Math.max(0,p);total+=p;if(i>j)h+=p;else if(i===j)d+=p;else a+=p;scores.push({score:`${i}:${j}`,prob:p})}if(!(total>0))return null;scores=scores.map(x=>({score:x.score,prob:x.prob/total})).sort((x,y)=>y.prob-x.prob);return{probabilities:normalize([h,d,a]),scores:scores.slice(0,8),probabilityUnit:'fraction'}}
function fraction(v){const x=n(v,NaN);if(!Number.isFinite(x)||x<0)return null;return x>1?x/100:x}
function normalizeScores(rows){const out=[];for(const x of rows||[]){const p=fraction(x.prob??x.probability);if(p!=null&&p<=1)out.push({score:String(x.score||'').replace('-',':'),prob:p})}return out.sort((a,b)=>b.prob-a.prob).slice(0,8)}
function sampleInfo(m){const fields=[m.sampleCount,m.historySample,m.history_samples,m.dataQuality?.sampleCount,m.dataQuality?.historySample,m.dataQuality?.samples,m.stats?.sampleCount];for(const v of fields)if(v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v)))return{known:true,count:Math.max(0,Number(v))};return{known:false,count:null}}
function buildFusion(m){const legacy=oldProb(m),market=marketProb(m),elo=eloProb(m),lh=n(m.score?.homeLambda??m.homeLambda,NaN),la=n(m.score?.awayLambda??m.awayLambda,NaN),poisson=matrix(lh,la,false),dc=matrix(lh,la,true),models=[];if(elo)models.push({key:'elo',name:'Elo',weight:.20,probabilities:elo,source:'upstream-elo'});if(poisson)models.push({key:'poisson',name:'GX/Poisson',weight:.25,probabilities:poisson.probabilities,source:'upstream-lambda'});if(dc)models.push({key:'dixonColes',name:'Dixon-Coles',weight:.15,probabilities:dc.probabilities,source:'upstream-lambda-rho--0.08'});if(legacy)models.push({key:'legacy',name:'旧接口模型',weight:.15,probabilities:legacy,source:'daily-jingcai'});if(market)models.push({key:'market',name:'体彩SP去水',weight:.25,probabilities:market,source:'official-sp'});const sum=models.reduce((s,x)=>s+x.weight,0),fused=[0,0,0];if(sum>0)for(const x of models)for(let i=0;i<3;i++)fused[i]+=x.probabilities[i]*x.weight/sum;const probabilities=normalize(fused)||legacy||market||[1/3,1/3,1/3],top=argmax(probabilities),consistency=models.length?models.filter(x=>argmax(x.probabilities)===top).length/models.length*100:0,scores=dc?.scores||poisson?.scores||normalizeScores(m.score?.topScores);return{version:'V16-P13',models:models.map(x=>({...x,probabilities:x.probabilities.map(v=>v*100),probabilityUnit:'percent',direction:['主胜','平局','客胜'][argmax(x.probabilities)]})),probabilities:probabilities.map(v=>v*100),probabilityUnit:'percent',modelCount:models.length,consistency:Number(consistency.toFixed(1)),weightsNormalized:true,scoreSource:dc?'dixon-coles':poisson?'poisson':'upstream',scores:scores||[],scoreProbabilityUnit:'fraction'}}
function riskWarnings(m,f){const p=f.probabilities,market=marketProb(m),draw=p[1],drawScores=(f.scores||[]).filter(x=>{const [h,a]=String(x.score).split(':').map(Number);return h===a}).reduce((s,x)=>s+n(x.prob),0)*100,drawSupport=(f.models||[]).filter(x=>x.direction==='平局').length,marketDraw=market?market[1]*100:null,gap=Math.abs(n(m.score?.homeLambda??m.homeLambda)-n(m.score?.awayLambda??m.awayLambda)),drawScore=clamp(draw*.9+drawScores*.7+drawSupport*5+(marketDraw??0)*.35+(gap<.35?12:gap<.7?6:0),0,100);let drawLevel=draw>=28||drawScore>=65?'高':draw>=24||drawScore>=50?'中':'低';const top=argmax(p),favorite=['主胜','平局','客胜'][top],nonFavorite=100-p[top],primaryRisk=top===0?(p[1]>=p[2]?'平局':'客胜'):top===2?(p[1]>=p[0]?'平局':'主胜'):(p[0]>=p[2]?'主胜':'客胜'),upsetScore=clamp(nonFavorite*.8+(f.consistency<60?15:0)+(m.handicap_pred?.recommendedLabel&&String(m.handicap_pred.recommendedLabel).includes(favorite)?0:8),0,100),upsetLevel=upsetScore>=68?'高':upsetScore>=52?'中':'低';return{version:'V16-P13',draw:{level:drawLevel,score:Number(drawScore.toFixed(1)),probability:Number(draw.toFixed(1)),drawScoreProbability:Number(drawScores.toFixed(1)),gxGap:Number.isFinite(gap)?Number(gap.toFixed(2)):null,modelSupport:drawSupport,reasons:[`融合平局概率${draw.toFixed(1)}%`,`平局比分合计${drawScores.toFixed(1)}%`,`${drawSupport}个模型首选平局`,marketDraw==null?'市场平局概率缺失':`市场平局${marketDraw.toFixed(1)}%`]},upset:{level:upsetLevel,score:Number(upsetScore.toFixed(1)),favorite,primaryRisk,reasons:[`非首选合计${nonFavorite.toFixed(1)}%`,f.consistency<60?'模型一致性偏低':`模型一致性${f.consistency.toFixed(0)}%`]}}}
function grade(m,f,w){const p=f.probabilities,ord=[...p].sort((a,b)=>b-a),lead=ord[0]-ord[1],si=sampleInfo(m),samples=si.count,reliability=n(m.dataQuality?.reliability??m.reliability),official=!!marketProb(m),quality=Math.min(100,(si.known?samples*4:0)+(official?25:0)+f.modelCount*8+(reliability>=45?10:0));let tier='C',reason='未达到B级门槛',baseTier='C';const common=official&&f.modelCount>=3&&f.consistency>=55&&quality>=40&&lead>=6&&reliability>=30;if(si.known&&samples>=6&&official&&f.modelCount>=4&&f.consistency>=70&&quality>=60&&lead>=10&&reliability>=45){baseTier='A';reason='样本、有效模型、一致性、官方SP与方向优势均达到A级'}else if(si.known&&samples>=3&&common){baseTier='B';reason='具备多模型和市场验证，但未达到A级完整要求'}else if(!si.known&&official&&f.modelCount>=4&&f.consistency>=70&&quality>=55&&lead>=10&&reliability>=45){baseTier='B';reason='历史样本字段缺失，凭多模型、官方SP和一致性暂列B级，不允许升级A级'}else if(si.known&&samples<3)reason=`历史样本不足（${samples}场）`;else if(!si.known)reason='历史样本字段缺失，等待上游补齐';else if(f.modelCount<3)reason=`有效模型不足（${f.modelCount}个）`;else if(f.consistency<55)reason=`模型一致性不足（${f.consistency.toFixed(0)}%）`;else if(!official)reason='缺少完整体彩SP';tier=baseTier;let riskDowngraded=false;if(w.draw.level==='高'&&tier==='A'){tier='B';riskDowngraded=true;reason+='；高平局预警降一级'}if(w.upset.level==='高'&&tier==='B'){tier='C';riskDowngraded=true;reason+='；高爆冷风险降一级'}return{version:'V16-P13',tier,reason,sampleCount:samples,sampleKnown:si.known,sampleStatus:si.known?`${samples}场`:'字段缺失',reliability,quality:Math.round(quality),lead:Number(lead.toFixed(1)),modelCount:f.modelCount,consistency:f.consistency,officialSp:official,baseTier,riskDowngraded}}
function enrich(m){const f=buildFusion(m),w=riskWarnings(m,f),g=grade(m,f,w);return{...m,homeWinProb:f.probabilities[0],drawProb:f.probabilities[1],awayWinProb:f.probabilities[2],spf:{...(m.spf||{}),homeWinProb:f.probabilities[0],drawProb:f.probabilities[1],awayWinProb:f.probabilities[2],tier:g.tier},score:{...(m.score||{}),topScores:f.scores,probabilityUnit:'fraction'},modelFusion:f,riskWarnings:w,calibratedTier:g.tier,grading:g}}
export async function onRequestGet(context){const u=new URL(context.request.url),q=new URLSearchParams();for(const[k,v]of u.searchParams)if(k!=='x')q.set(k,v);if(!q.has('days'))q.set('days','4');try{const r=await fetch(`${UPSTREAM}?${q}`,{headers:{accept:'application/json','user-agent':'Mozilla/5.0'}}),text=await r.text(),payload=JSON.parse(text);if(!r.ok||payload.success===false)return json({success:false,error:payload.error||`upstream ${r.status}`,matches:[]},502);const matches=(Array.isArray(payload.matches)?payload.matches:[]).map(enrich),gradeCounts=matches.reduce((a,m)=>(a[m.grading.tier]++,a),{A:0,B:0,C:0});return json({...payload,success:true,total:matches.length,matches,gradeCounts,modelVersion:'V16-P13 Sample & Percent Fix',strategyAudit:{elo:'active-when-upstream-elo-exists',gxPoisson:'active-when-lambda-exists',dixonColes:'active-when-lambda-exists',legacy:'active-when-legacy-probability-exists',sportteryMarket:'active-when-three-sp-exist',drawWarning:'fixed-percent-and-multi-signal',upsetWarning:'risk-only',sampleHandling:'missing-is-not-zero-b-tier-cap',scoreProbability:'fraction-0-to-1',calibration:'collecting-full-review-samples',dynamicWeights:'disabled-until-sufficient-sample'}})}catch(e){return json({success:false,error:e.message,matches:[]},502)}}
